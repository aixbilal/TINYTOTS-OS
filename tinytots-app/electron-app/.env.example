# Required for production builds. Must match POS_API_SECRET in backend/.env.
# Dev falls back to a well-known default with a console warning if unset.
VITE_POS_API_SECRET=change-me-to-a-long-random-string
