# file: protocol dependencies (currently only vendor/esbuild-wasm-stub, see
# package.json) are packed and installed as a real copy in node_modules
# instead of a symlink. Deterministic on every platform and keeps npm from
# creating a node_modules symlink at all, which matters on Windows.
install-links=true
